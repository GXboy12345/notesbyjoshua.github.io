---
---
<script>
  import { renderKatexIn } from '../lib/katex-client';

  function run() {
    const el = document.querySelector('.doc-main article.prose');
    if (el instanceof HTMLElement) renderKatexIn(el);
  }

  run();
  document.addEventListener('astro:page-load', run);
</script>
